#!/bin/bash

# Ensure an argument was provided
if [ -z "$1" ]; then
    echo "Usage: ./video.sh <folder_name>"
    exit 1
fi

# Run the cleanup binary, passing the folder name
cargo run --quiet --bin video -- "$1"
